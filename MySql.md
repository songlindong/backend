# 存储引擎

## 执行一条SQL的过程是什么？

1. 客户端与服务器建立连接

- TCP三次握手
- 连接器身份验证
  验证客户端的用户名、密码、主机IP是否合法
  验证通过后，查询用户的权限表(mysql.user等)，获取该用户的所有权限
  为该连接分配线程资源，维护连接状态（如自动提交、事务隔离级别）
- 连接池复用
  为避免频繁创建销毁连接的开销，生产环境通常使用连接池（如HikariCP）,复用已建立的连接

2. 查询缓存（MySQL8.0已移除）

- MySQL5.7及之前的版本会先检查查询缓存：将SQL语句进行哈希计算，与缓存中的key对比
- 如果命中缓存且用户有查询权限，直接返回缓存的结果集
- 如果未命中，继续后续流程；执行完成后，会将结果集存入查询缓存
  为什么8.0移除了查询缓存
- 缓存失效非常频繁：只要表有任何更新(INSERT/UPDATE/DELETE),该表的所有查询缓存都会被清空
- 对于写多读少的系统，查询缓存的命中率极低，反而增加了系统开销

3. 解析器(Parser) 将SQL字符串转为抽象语法树

- 解析器的核心工作是词法分析+语法分析，将人类可读的SQL字符串转换为计算机可处理的结构化数据
  词法分析：将SQL字符串拆分成一个个“token” (关键字、标识符、字面量、运算符等)
  语法分析：根据MySQL的语法规则，将token组合成抽象语法树(AST)

4. 预处理器：验证与语义检查
   预处理器对解析器生成的AST进行语义验证和预处理，解决语法分析无法处理的问题：

- 检查数据库对象是否存在：验证表名、列名是否存在，是否有歧义（如多表联查时的列名冲突）
- 权限检查：验证当前用户是否有执行该SQL的权限(如SELECT、INSERT权限)
- 展开视图：如果查询的是视图，将视图定义的SQL语句展开，替换为实际的表查询
- 解析子查询：将子查询转换为更易优化的形式
- 常量表达式计算：提前计算常量表达式的值

5. 优化器：生成最优执行计划

- 优化器的主要工作
  逻辑优化：对于SQL进行等价转换，生成更高效的逻辑执行计划
- 条件下推：将WHERE条件尽可能推到最底层，提前过滤数据
- 子查询优化：将子查询转换为连接查询
- 消除冗余：如删除不必要的括号、合并相同条件
  物理优化：为逻辑执行计划选择具体的物理执行方式
  索引选择：决定使用哪个索引，或者是否全表扫描
  表连接顺序：多表联查时，决定先查哪个表（驱动表），后查哪个表(被驱动表)
  连接算法选择：选择嵌套循环连接、哈希连接或排序合并连接
  排序方式选择：选择使用索引排序还是文件排序

6. 执行器：调用存储引擎执行计划
   执行流程（以SELECT语句为例）

- 执行器先判断用户是否有查询该表的权限（虽然预处理器已经检查过，但执行器会再次检查）
- 打开表，根据表的存储引擎类型，调用对应的存储引擎接口
- 按照执行计划的步骤，逐行（或批量）从存储引擎读取数据
- 对读取的数据进行过滤、排序、连接、聚合等操作
- 将最终结果集返回给客户端

7. 存储引擎：真正读写数据
   存储引擎是MySQL的“数据管家”，负责数据的存储、提取、事务管理、锁机制等。MySQL支持多种存储引擎，最常用的是InnDB
   InnDB的核心执行流程

- 缓冲池检查：先在内存缓冲池中查询需要的数据页
- MVCC读取：如果是普通查询（非锁定读），InnoDB会通过**多版本并发控制**读取数据的快照版本，避免加锁
- 索引查找：如果使用了索引，通过B+树索引快速定位到数据行

8. 返回结果给客户端

## MySQL 为什么InnoDB是默认引擎

- InnoDB 引擎在事务支持、并发性能、崩溃恢复等方面具有优势，因此被MySQL选择为默认的存储引擎
- 事务支持：InnoDB引擎提供了对事务的支持，可以进行ACID(原子性、一致性、隔离性、持久性、)属性的操作。Myisam存储引擎是不支持事务的
- 并发性能：InnoDB引擎采用了行级锁定的机制，可以提供更好的并发性能，Myisam存储引擎只支持表锁，锁的粒度比较大
- 崩溃恢复：InnoDB引擎通过redo log日志实现了崩溃恢复，可以在数据库发生异常情况(如断电)时，通过日志文件进行恢复，保证数据的持久性和一致性，
  MyISAM是不支持崩溃恢复的

## 说一下mysql的innodb与MylSAM的区别？

- 事务：InnoDB支持事务，MyISAM不支持事务，这是MySQL将默认存储引擎从MyISAM变成InnoDB的重要原因之一
- 索引结构：InnoDB是聚簇索引，MycISAM是非聚簇索引。聚簇索引的文件存放在主键索引的叶子节点上，因为InnoDB必须要有主键，通过主键索引
  效率更高。但是辅助索引需要两次查询，先查询到主键，然后再通过主键查询到数据，因此，主键太大，其他索引也都会很大。而MyIsam是非聚簇
  索引，数据文件是分离的，索引保存的是数据文件的指针。主键索引和辅助索引是独立的
- 锁粒度：InnoDB最小的锁粒度是行锁，MyISAM最小的锁粒度是表锁。一个更新语句会锁住整张表，导致其他查询和更新都会被阻塞，因此并发访问受限
- count的效率：InnoDB不保存表的具体行数，执行select count(\*) from table时需要全表扫描。而MyISAM用一个变量保存了整个表的行数，执行
  上述语句时只需要读出该变量即可，速度很快
